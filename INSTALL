boardtt installation
================================================


Python ``setuptools`` package is required to install boardtt.


Use the following command line to install boardtt from sources directory (containing setup.py):

    easy_install .

or

    python setup.py install


Alternatively you can install an up-to-date version of boardtt from PyPI:

    easy_install boardtt


Note: `easy_install` is bundled with ``setuptools``.
